<UserControl x:Class="OrderingSpecialEquipment.Views.SettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:OrderingSpecialEquipment.Views"
             xmlns:converters="clr-namespace:OrderingSpecialEquipment.Converters"
             mc:Ignorable="d"
             d:DesignHeight="400" d:DesignWidth="600"
             d:DataContext="{d:DesignInstance Type=local:SettingsViewModel, IsDesignTimeCreatable=True}">
    <UserControl.Resources>
        <converters:Converters.BoolToYesNoConverter x:Key="BoolToYesNo"/>
        <converters:Converters.BoolToVisibilityConverter x:Key="BoolToVis"/>
        <Style x:Key="ThemedButtonStyle" BasedOn="{StaticResource ThemedButtonStyle}" TargetType="Button" />
        <Style x:Key="ThemedLabelStyle" BasedOn="{StaticResource {x:Type Label}}" TargetType="Label" />
        <Style x:Key="ThemedComboBoxStyle" BasedOn="{StaticResource {x:Type ComboBox}}" TargetType="ComboBox" />
        <Style x:Key="ThemedTextBoxStyle" BasedOn="{StaticResource ThemedTextBoxStyle}" TargetType="TextBox" />
    </UserControl.Resources>

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>

        <Label Grid.Row="0" Grid.Column="0" Content="Тема оформления:" Style="{StaticResource ThemedLabelStyle}"/>
        <ComboBox Grid.Row="0" Grid.Column="1" Grid.ColumnSpan="2" 
                  ItemsSource="{Binding AvailableThemes}" 
                  SelectedValue="{Binding SelectedTheme, UpdateSourceTrigger=PropertyChanged}"
                  Style="{StaticResource ThemedComboBoxStyle}" Margin="0,0,0,10"/>

        <Label Grid.Row="1" Grid.Column="0" Content="Тип подключения:" Style="{StaticResource ThemedLabelStyle}"/>
        <ComboBox Grid.Row="1" Grid.Column="1" Grid.ColumnSpan="2" 
                  ItemsSource="{Binding AvailableConnectionTypes}" 
                  SelectedValue="{Binding ConnectionType, UpdateSourceTrigger=PropertyChanged}"
                  Style="{StaticResource ThemedComboBoxStyle}" Margin="0,0,0,10"/>

        <Label Grid.Row="2" Grid.Column="0" Content="Строка подключения:" Style="{StaticResource ThemedLabelStyle}"/>
        <TextBox Grid.Row="2" Grid.Column="1" Grid.ColumnSpan="2" 
                 Text="{Binding ConnectionString, UpdateSourceTrigger=PropertyChanged}" 
                 TextWrapping="Wrap" AcceptsReturn="True" Height="60" 
                 Style="{StaticResource ThemedTextBoxStyle}" Margin="0,0,0,10"/>

        <StackPanel Grid.Row="3" Grid.Column="0" Grid.ColumnSpan="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
            <Button Content="Тест подключения" Command="{Binding TestConnectionCommand}" Style="{StaticResource ThemedButtonStyle}" Margin="0,0,5,0"/>
            <Button Content="Сохранить" Command="{Binding SaveSettingsCommand}" Style="{StaticResource ThemedButtonStyle}" Margin="0,0,5,0"/>
        </StackPanel>
    </Grid>
</UserControl>
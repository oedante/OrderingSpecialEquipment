<Window x:Class="OrderingSpecialEquipment.Views.SplashScreen"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        WindowStyle="None"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterScreen"
        AllowsTransparency="True"
        Background="Transparent"
        Width="600"
        Height="400">
    <Border Background="White" CornerRadius="10" BorderBrush="#E0E0E0" BorderThickness="1">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <StackPanel Grid.Row="0" VerticalAlignment="Center" HorizontalAlignment="Center">
                <TextBlock Text="Система заказа специальной техники" 
                           FontSize="24" 
                           FontWeight="Bold" 
                           Foreground="#0078D7"
                           HorizontalAlignment="Center"/>

                <Ellipse Width="60" Height="60" 
                         Stroke="#0078D7" 
                         StrokeThickness="2" 
                         Margin="0,20,0,0"
                         RenderTransformOrigin="0.5,0.5">
                    <Ellipse.RenderTransform>
                        <RotateTransform Angle="0"/>
                    </Ellipse.RenderTransform>
                    <Ellipse.Triggers>
                        <EventTrigger RoutedEvent="Loaded">
                            <BeginStoryboard>
                                <Storyboard>
                                    <DoubleAnimation Storyboard.TargetProperty="(Ellipse.RenderTransform).(RotateTransform.Angle)"
                                                     From="0" To="360" Duration="0:0:2" RepeatBehavior="Forever"/>
                                </Storyboard>
                            </BeginStoryboard>
                        </EventTrigger>
                    </Ellipse.Triggers>
                </Ellipse>
            </StackPanel>

            <TextBlock Grid.Row="1" 
                       Text="Загрузка приложения..." 
                       FontSize="12" 
                       Foreground="#909090"
                       HorizontalAlignment="Center"
                       Margin="0,0,0,20"/>
            <Image HorizontalAlignment="Left" Height="33" Margin="488,10,0,0" VerticalAlignment="Top" Width="100" Source="/Pictures/yamal_logo.png"/>
        </Grid>
    </Border>
</Window>